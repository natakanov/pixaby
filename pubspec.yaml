name: pixaby
description: "Small client for Pixaby API"

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  cached_network_image: ^3.3.0
  collection: ^1.15.0
  dio: ^5.0.0
  elementary: ^3.0.0
  elementary_helper: ^1.0.1
  fast_immutable_collections: ^10.0.0

  flutter:
    sdk: flutter

  flutter_svg: ^2.0.0
  freezed_annotation: ^2.1.0
  go_router: ^12.0.0
  intl: ^0.18.0
  json_annotation: ^4.8.1
  photo_view: ^0.15.0
  provider: ^6.0.5

dev_dependencies:
  build_runner: ^2.1.2

  flutter_test:
    sdk: flutter

  freezed: ^2.1.0
  json_serializable: ^6.1.0
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true


